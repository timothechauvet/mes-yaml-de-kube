apiVersion: apps/v1
kind: Deployment
metadata:
  name: yasssf-frontend
  namespace: default
  labels:
    app: yasssf-frontend
  annotations:
    keel.sh/policy: force
    keel.sh/trigger: webhook
spec:
  replicas: 2
  selector:
    matchLabels:
      app: yasssf-frontend
  template:
    metadata:
      labels:
        app: yasssf-frontend
    spec:
      nodeSelector:
        kubernetes.io/arch: arm64
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: yasssf-frontend
          image: ghcr.io/timothechauvet/ai-ready-flashcard-maker:main
          imagePullPolicy: Always
          ports:
            - containerPort: 80
          env:
            - name: NODE_ENV
              value: production
            - name: PORT
              value: "80"
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
