apiVersion: v1
kind: Service
metadata:
  name: sdjsac-frontend
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: sdjsac-frontend
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
